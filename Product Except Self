def productExceptSelf(self, nums):
        left = [1]
        right = [1]
        arr = []
        for i in range(len(nums) - 1):
            if(len(left) != len(nums)):
                left.append(nums[i] * left[i])
                right.insert(0, (nums[(len(nums)-1) - i] * right[0]))
        for j in range(len(left)):
            arr.append(left[j] * right[j])
        return arr
